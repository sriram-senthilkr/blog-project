<%- include("partials/header"); -%>

    <h1>Home!</h1>

    <p><%= startingContent %></p>
    
    <% for (var i=0; i < posts.length; i++) { %>
        <tr>
            <td><%= posts[i].title %></td>
            <td><%= posts[i].content %></td>
        </tr>
    <% } %> 

    <% for (var i=0; i < posts.length; i++) { %>
        <td><%= console.log(posts[i].title) %></td>
    <% } %> 

<%- include("partials/footer"); -%>
